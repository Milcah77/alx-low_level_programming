#include "main.h"

/**
 * print_sign - print the sign of a number
 * @n: the number to be printed
 * Return: 1, 0, and -1
 */
int print_sign(int n)
{
	if (n > o)
	{
		_putchar('+');
		return (1);
	}
	els if (n == 0)
	{
		_putchar('0');
		return (0);

	}
	else
	{
		_putchar('-');
		return (-1);
	}

}
